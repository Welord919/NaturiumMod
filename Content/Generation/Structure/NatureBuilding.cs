using Microsoft.Xna.Framework;
using NaturiumMod.Content.Items.General.Placeable;
using NaturiumMod.Content.Items.PreHardmode.Materials;
using NaturiumMod.Content.Items.PreHardmode.Weapons;
using Terraria;
using Terraria.GameContent.Generation;
using Terraria.ID;
using Terraria.ModLoader;
using Terraria.WorldBuilding;
/*
using StructureHelper;

namespace NaturiumMod
{
    public class NatureBuilding : ModSystem
    {
        public override void PostWorldGen()
        {
            for (int i = 0; i < 4; i++)
            {
                // Pick a location
                int x = WorldGen.genRand.Next(200, Main.maxTilesX - 200);
                int y = WorldGen.genRand.Next((int)Main.rockLayer, Main.maxTilesY - 200);

                // Place your structure
                Generator.GenerateStructure("NatureBuilding", new Point16(x, y), Mod);
            }
        }
        
    }
}
*/