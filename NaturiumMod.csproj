<?xml version="1.0" encoding="UTF-8"?>
<Project Sdk="Microsoft.NET.Sdk">
    <!-- Import tModLoader mod properties -->
    <!-- Windows (relative to repo) -->
    <Import Project="..\tModLoader.targets"
            Condition="'$(OS)' == 'Windows_NT' and Exists('..\tModLoader.targets')" />
    <!-- Linux (This stinky ass path because Alexis's system is different) -->
    <Import Project="/mnt/secondary/SteamLibrary/steamapps/common/tModLoader/tMLMod.targets"
            Condition="'$(OS)' != 'Windows_NT' and Exists('/mnt/secondary/SteamLibrary/steamapps/common/tModLoader/tMLMod.targets')" />
    <!-- General -->
    <PropertyGroup>
        <TargetFramework>net10.0</TargetFramework>
    </PropertyGroup>
    <ItemGroup>
        <PackageReference Include="CloudinaryDotNet"
                          Version="1.26.2" />
    </ItemGroup>
    <ItemGroup>
        <Folder Include="Content\Items\Projectiles\PreHardmodeProj\" />
    </ItemGroup>
    <ItemGroup>
        <None Include="Content\Items\Placeable\Camelia.cs" />
    </ItemGroup>
    <!-- References -->
</Project>